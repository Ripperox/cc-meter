---
description: Show your Claude Code token/cost usage trends across sessions (cc-meter)
allowed-tools: Bash(python3:*)
---

Use the Bash tool to run this command exactly:

    python3 "${CLAUDE_PLUGIN_ROOT}/scripts/report.py"

Then show the script's stdout to the user **verbatim** inside a code block. Do not summarize, analyze, reformat, or add any commentary — just display the report output as-is.
